<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:background="#F0F5F7"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <include layout="@layout/nav_header" />
    </LinearLayout>

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="start"
        android:layout_weight="2"
        android:background="#00ffffff"
        android:fitsSystemWindows="true"
        app:itemBackground="@drawable/nav_content_background_color"
        app:itemIconTint="@color/nav_content_item_color"
        app:itemTextColor="@color/nav_content_item_color"
        app:menu="@menu/nav_content"
        android:theme="@style/nav_Style"></android.support.design.widget.NavigationView>


</LinearLayout>